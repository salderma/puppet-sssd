# File Managed by Puppet
# Sample template for sssd::configfile
# Add parameters to the define sssd::configfile to have them here
# as variables. Alternatively use the 'options' generic parameter

# Custom Generic Options
opt_a = <%= @options['opt_a'] %>
opt_b = <%= @options['opt_b'] %>
